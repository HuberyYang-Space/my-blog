# 评估记录:是否引入 @nuxt/ui(结论:不采用,已回滚)

## 背景

2026-08-11 起因是文章大纲(TOC)自定义实现(`app/components/PostOutline.vue`)反复出滚动/高亮相关的 bug,借此评估把项目里能用现成组件库解决的部分整体换成 `@nuxt/ui`,长期降低自己维护自定义组件的心智负担。经过一次系统性访谈确定范围后,实际动手做了 Stage 0(基线确认)、Stage 1(装 `@nuxt/ui`/`tailwindcss`,spike 大纲替换方案)、Stage 6(提示框被迫提前迁移),提交为 commit `5006d5d`。

**2026-08-12 决定回滚**,已用 `git revert 5006d5d` 完整撤销(revert commit `d733837`),工作区确认与引入前的 `ea98577` 完全一致(`git diff ea98577 HEAD` 无输出)。

## 结论:不值得引入,原因

1. **最初触发这次评估的目标(大纲 TOC 反复出 bug)没有被解决**。`ContentToc`(NuxtUI 的大纲组件)经 spike 实测后放弃:
   - 滚动机制本身没问题(它把滚动完全委托给本项目 `[slug].vue` 里已有的 `route.hash` watcher,不会复现之前"内容上移留白"的 bug)。
   - 但会把现在的平滑滚动**退化成瞬间跳转**——要恢复平滑滚动就得改 `[slug].vue`(它同时服务于搜索跨文章跳转的合法场景,不能随便动)。
   - 高亮功能**实测完全不工作**,且深入排查(手动补触发 NuxtUI 内部初始化钩子、确认 `IntersectionObserver.observe()` 已正确注册)后依然找不到根因。自动化浏览器测试环境本身有标签页失焦导致 `IntersectionObserver` 不触发的已知干扰,但这条已经改用真实、手动聚焦的浏览器窗口独立复现,排除了环境噪音的可能。
   - 结论:这部分收益是 0,大纲继续用 2026-08-11 已经修好的 `PostOutline.vue`(FAQ 滚动 bug 修复,与 NuxtUI 无关,继续保留)。

2. **唯一落地的收益(提示框 Callout 换成 NuxtUI 组件)不是主动选择,是被迫的**。装上 `@nuxt/ui` 那一刻,它检测到 `@nuxt/content` 就会**无条件**注册自己的同名 Prose Callout 组件族(`::note`/`::tip`/`::warning`/`::caution`),没有开关能只关掉这几个、保留其他 Prose 组件——本项目自己的 `app/components/mdc/Callout.vue` + 4 个语义包装被静默架空,构建期产物断言直接失败。

   这件事本身就是一个警示信号:**引入 NuxtUI 的初衷是"减少静默失败的风险",但它第一次接触就触发了这个项目 `CLAUDE.md` 里最警惕的那类失败模式**("构建成功、页面照常渲染、只是某块东西凭空消失")。这次是我们自己在场、能及时发现并修复,但如果是日常开发中不经意加了个新依赖触发同样的接管,不一定能这么快抓到。

3. **成本明显偏高,和"减少心智负担"的初衷相反**:
   - 会形成两套并存的主题系统(项目自己的 `--c-*` 语义变量 vs NuxtUI 的 `--ui-*`/`app.config.ts` 颜色系统),以后配色改动要两边都想到。
   - 网上查到的资料/记忆里的信息多次和实际装的版本对不上(`ui.global` 这个配置项在实际装的 `@nuxt/ui@4.10.0` 里根本不存在;`ContentToc` 实际组件名要带 `U` 前缀;Prose 自动接管同名组件这个机制文档完全没提)。说明这是个变化较快、文档滞后的库,以后升级版本时同类"文档说的和实际不一样"的意外大概率还会再发生。
   - 图标离线打包不是"装了对应 `@iconify-json/*` 包就自动生效"——NuxtUI 只自动信任它自己默认图标集合来源的那个 collection(lucide),换成本项目统一在用的 Phosphor 需要额外配置,不配的话会静默退化成运行时向 `api.iconify.design` 发外部请求,构建不报错,不看日志发现不了。
   - 依赖体积变重(Tailwind v4 + `@nuxt/ui` + `reka-ui` 等一整条传递依赖),而 UnoCSS 原来跑得好好的,零投诉。
   - 一整个会话的时间成本花在排查上述几件事上。

## 如果以后要重新考虑

不是"永远不再考虑",而是"现在的证据不支持"。以后如果出现以下情况之一,值得重新评估:
- 大纲(TOC)这类交互组件本身有了明确、具体的新需求(不只是"想少维护代码"),现成组件库能直接解决而不是像这次一样评估后发现做不到。
- `@nuxt/ui` 版本更成熟、Prose 组件族有了"选择性禁用某几个内置组件、保留其余"的配置项(这次遇到的核心阻塞点)。
- 项目本身要做一次更大的视觉改版,愿意承担引入 Tailwind CSS 作为第二套/替代原子化 CSS 引擎的整体成本(这次评估里 Tailwind 完全是"因为 `@nuxt/ui` 需要"才捎带引入的,没有独立于 NuxtUI 的理由)。

这次是直接在 `main` 上装的依赖,之所以能干净退出,靠的是两点,重新评估时照样要守住:一是把"装依赖 + 被迫的连带改动"收成一个独立、自包含的 commit,不要和其他改动混在一起,回滚时才能用 `git revert` 精确撤销、不留残余(已用 `git diff` 验证过完全一致);二是**装上之后先跑一遍真实验证再继续往下做**,而不是先把所有 Stage 都做完再评估——这次能在只做了 Stage 1/6 时就叫停,没有让沉没成本继续累积,正是因为装完依赖立刻就去 spike 了最不确定的那个点(大纲组件)。
