---
/**
 * 页面底部的"返回"链接。
 *
 * 关于页、标签归档页、文章详情页三处原本各写了一遍同样的 markup 与样式,
 * 只有 href 和文案不同 —— 它们是同一个 UI 概念的三次实例,收进组件里。
 * 左箭头是这个概念的固定组成部分,写死在组件内,调用方只给目标和文案。
 */
interface Props {
  href: string
}

const { href } = Astro.props
---

<a href={href} class="tinter text-sm text-text-mute">
  ← <slot />
</a>
